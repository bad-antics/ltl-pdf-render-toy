// examples/demo.ltl — runnable demo for ltl_pdf_render_toy

import std::io
use ltl_pdf_render_toy as lib

fn main() {
    io::println("ltl_pdf_render_toy demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}